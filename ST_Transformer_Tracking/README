# ST Transformer Tracking

This workspace now uses the PyTorch ST-Transformer training and validation
pipeline only. Old non-PyTorch scripts and checkpoints have been removed.

Use `README.md` for the current project layout and commands.
